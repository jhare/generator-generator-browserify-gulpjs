{
  "name": "<%= projectName %>",
  "version": "<%= initialVersion %>",
  "dependencies": {
  },

  "devDependencies": {
    "gulp": "3.6.x",
    "browserify": "3.44.x",
    "gulp-browserify": "0.5.x",
    "gulp-rename:" "1.2.x"
  }
}
